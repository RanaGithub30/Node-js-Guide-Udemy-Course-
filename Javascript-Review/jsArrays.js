let hobbies = ['Cricket', 'Foot Ball', 'Book Reading'];

// for loop
for(let hobby of hobbies){
     console.log(hobby);
}

// map function => used for modify existing records
// for example

console.log(hobbies.map(hobby => 'Hobby :'+hobby));